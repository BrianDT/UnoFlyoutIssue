<Page
    x:Class="FlyoutIssue.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlyoutIssue"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border BorderBrush="Black" BorderThickness="1" Padding="8"
                Background="LightGray" CornerRadius="2"
                Tapped="Open_Flyout">
          <TextBlock Text="Tap me" Margin="20" FontSize="30" />

          <FlyoutBase.AttachedFlyout>
            <Flyout x:Name="flyout" LightDismissOverlayMode="On">
              <Button Command="{x:Bind VM.DoStuffCommand}" HorizontalAlignment="Right">
                <Button.Template>
                  <ControlTemplate>
                    <Border BorderBrush="Blue" BorderThickness="1" Padding="16" Background="LightBlue" CornerRadius="8">
                      <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Contact" />
                        <TextBlock Text="Then Tap this" Margin="8,0,0,0"/>
                      </StackPanel>
                    </Border>
                  </ControlTemplate>
                </Button.Template>
              </Button>
            </Flyout>
          </FlyoutBase.AttachedFlyout>
        </Border>

            <Button Content="Enable Manual dismiss" Tapped="Enable_Manual_Dismiss" Margin="0,24,0,0"/>
      </StackPanel>
    </Grid>
</Page>
